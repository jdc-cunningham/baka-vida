import {
  SafeAreaView,
  ScrollView,
  View,
} from 'react-native';

export const FinancesScreen = ({navigation, route}) => {
    return (
        <SafeAreaView>
            <ScrollView>
                <View>

                </View>
            </ScrollView>
        </SafeAreaView>
    )
};
